---
import SidebarLayout from "@/components/SidebarLayout";
import { Menu, Moon} from "lucide-react";
import { Button } from "@/components/ui/button"
import { ModeToggle } from "../components/ModeToggle"
import '../styles/global.css';
---

<script is:inline>
	const getThemePreference = () => {
		if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
			return localStorage.getItem('theme');
		}
		return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
	};
	const isDark = getThemePreference() === 'dark';
	document.documentElement.classList[isDark ? 'add' : 'remove']('dark');
 
	if (typeof localStorage !== 'undefined') {
		const observer = new MutationObserver(() => {
			const isDark = document.documentElement.classList.contains('dark');
			localStorage.setItem('theme', isDark ? 'dark' : 'light');
		});
		observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });
	}
</script>


<!doctype html>
<html lang="de">
  <head>
    <meta name="color-scheme" content="light dark" />
    <title>Astro Basics</title>
  </head>

  <body class="min-h-screen antialiased bg-background text-foreground">
<SidebarLayout client:only="react">
  <div class="flex flex-col w-full">
    <!-- Extra Header nur für ModeToggle -->
    <header class="sticky top-0 z-40 w-full   backdrop-blur py-3 mb-4">
      <div class="flex items-center justify-end gap-2">
        <ModeToggle client:only="react" />
      </div>
    </header>

    <!-- Page Content -->
    <main class="flex-1">
      <slot />
    </main>
  </div>
</SidebarLayout>
  </body>
</html>